server:
  port: **
spring:
  application:
    name: **
  cloud:
    config:
      server:
         git:
           uri: **(git url)
           username: **
           password: *****
eureka:
  instance:
    hostname: ***
    non-secure-port: **
  client:
    service-url:
      defaultZone: http://***:**/eureka/
endpoints:
  restart:
    enabled: true
